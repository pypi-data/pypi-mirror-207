
## Entrypoints for `{{project.name}}`

-------------------------------------------------------------------------------

These are scripts/commands that ship with the package `{{python.package.name}}`:

{% for item in entrypoints %}
{%- set dotpath = item.module + '.' + item.entrypoint %}
* [`{{item.bin_name}}`](/docs/cli/{{item.bin_name}}.md) @ [`{{dotpath}}`](/docs/api#{{item.module.replace('.', '')}})
{%-endfor%}

Modules that use `__main__`:

{% for fname,metadata in python.entrypoints.items() %}
* [`python -m{{metadata.dotpath}}`](/docs/cli/{{metadata.dotpath}}.md) @ [`{{f}}`](/{{metadata.path}}) @
{%-endfor%}
