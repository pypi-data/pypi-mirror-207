[tool.poetry]
name = "vidcardshare"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
include = [
    {'path' = 'assets/*'}
]

[tool.poetry.dependencies]
python = "^3.10"
typer-tinydb = "^0.1.4"
jinja2 = "^3.1.2"
pytube = "^15.0.0"

[tool.poetry.scripts]
vidcardshare = "vidshare.main:app"
vidcsh = "vidshare.main:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
