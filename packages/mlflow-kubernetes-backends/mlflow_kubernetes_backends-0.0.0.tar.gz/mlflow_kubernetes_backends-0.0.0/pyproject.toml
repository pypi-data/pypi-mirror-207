[tool.poetry]
name = "mlflow-kubernetes-backends"
version = "0.0.0"
readme = "README.md"
description = ""
authors = []
packages = [{include = "mlflow_kubernetes_backends"}]

[tool.poetry.dependencies]
python = "^3.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
