from setuptools import setup

setup(
    name="serdeserc",
    version="0.0",
    description="don't use it, even if you have to",
    packages=["serdeserc"],
    author_email="kirkat6@gmail.com",
    zip_safe=False,
)
