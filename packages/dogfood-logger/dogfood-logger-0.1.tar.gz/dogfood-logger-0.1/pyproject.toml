[tool.poetry]
name = "dogfood-logger-package"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "dogfood_logger_package"}]

[tool.poetry.dependencies]
python = "^3.9"
setuptools = "^67.7.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
