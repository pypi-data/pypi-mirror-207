[tool.poetry]
name = "mineterm"
version = "0.0.0"
description = "A simple Minecraft launcher for the terminal."
authors = ["mudkipdev <mudkip@mudkip.dev>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
textual = "^0.23.0"


[tool.poetry.group.dev.dependencies]
black = "^23.3.0"
textual = {extras = ["dev"], version = "^0.23.0"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
