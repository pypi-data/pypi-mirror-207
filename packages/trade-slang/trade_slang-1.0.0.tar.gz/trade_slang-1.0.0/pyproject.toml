[tool.poetry]
name = "trade-slang"
version = "1.0.0"
description = ""
authors = ["Rodion Gudz <rodion@gudz.ml>"]
readme = "README.md"
packages = [{include = "slang"}]

[tool.poetry.scripts]
slang = "slang.cli:cli"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.30.0"
rich-click = "^1.6.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
