from setuptools import setup, find_packages

setup(
    name='square_vilavr',
    version='0.1.0',
    description='A simple package that calculates the square of the sum of two numbers',
    author='Your Name',
    packages=find_packages(),
    install_requires=[],
    tests_require=['unittest'],
)
